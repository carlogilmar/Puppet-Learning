package{ 'tmux':
        ensure => installed,
}
package{ 'git':
        ensure => installed,
}
package{ 'htop':
        ensure => installed,
}
package{ 'zsh':
        ensure => installed,
}
package{ 'fish':
        ensure => installed,
}
